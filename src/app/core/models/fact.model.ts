export interface Fact {
    NSS: number;      // Nombre de séances de sensibilisation
    Peduca: number;   // Participation éducation (0 ou 1)
    NI: number;       // Niveau d'instruction (0-4)
    NAM: number;      // Nombre d'activités mensuelles
    AgeMere: number;  // Âge de la mère
    AAMA: number;     // Alimentation adéquate minimale acceptable (0 ou 1)
    MAQA: number;     // Méthodes d'amélioration qualité aliments (0 ou 1)
    SML: number;      // Surveillance malnutrition légère (0 ou 1)
}